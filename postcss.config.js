export default {
    plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
    },
    // Configured to use @tailwindcss/postcss plugin
    // (Assuming this is the intended tailwindcss PostCSS plugin usage)
    // No closing bracket needed here since the plugins object and config closure
    // are above. Remove this stray closing bracket.
}